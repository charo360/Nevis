/**
 * Firebase disabled stub
 * You decided to move entirely to Supabase. This file now exports no-op stubs
 * so existing imports won't crash during the transition.
 */

// Minimal stubs to satisfy existing imports without pulling firebase packages
export const auth: any = null;
export const storage: any = null;
export const firestore: any = null;
export const firebaseConfig: any = {};

const app: any = null;
export default app;
